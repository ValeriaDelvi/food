import React from "react";
import { View, Text,StyleSheet } from "react-native";

const Component = ()=>{
    return (
    <View>
        <Text>Sono component</Text>
    </View>
    );
};

const styles = StyleSheet.create({});

export default Component;