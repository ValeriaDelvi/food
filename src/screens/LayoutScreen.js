import React from "react";
import { View, Text,StyleSheet } from "react-native";

const Screen = ()=>{
    return (
    <View>
        <Text>Sono Screen</Text>
    </View>
    );
};

const styles = StyleSheet.create({});

export default Screen;